import React from 'react';

import Nav from './Nav';
import Form from './Form';

const Header = () => {
  return (
    <header>
      <Form />

      <Nav />
    </header>
  );
}

export default Header;